(ns aoc2020.day11
  (:require [aoc2020.util :as util]))

(defn main
  "Day 11 of Advent of Code 2020: 
      lein run day11 <input> <preamble length>
  where <input> is a filename in project resources/"
  [[filename pream-len]]
  ;(let [(util/read-lines filename))
  ;(let [(util/slurp-resource filename))
         )
